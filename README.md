# fyfi
User friendly caching proxy service for the PyPI Python package repository

# Developers Notes

1. checkout the source repository
1. run `go generate ./schema`
1. run `go test`

Confirm that all unit tests succeed before proceeding to contribute changes.
